# Data Models: Bookstore Api

**Generated**: 2025-12-14
**Source**: Reverse-engineered from project models
**Total Models**: 3

This document provides comprehensive documentation for all data models in the Bookstore Api application.

---

## Overview

The Bookstore Api uses 3 data models to represent the domain:

- **Order** - 9 fields
- **OrderItem** - 5 fields
- **Book** - 11 fields

---

## Model Descriptions


### Order

**Location**: `src/main/java/com/example/bookstore/model/Order.java`
**Fields**: 9

**Description**:
Order entity representing a customer order.

Contains order details including customer information, items ordered,
and order status.

**Fields**:

- `id` (`Long`)
- `customerName` (`String`)
- `customerEmail` (`String`)
- `shippingAddress` (`String`)
- `orderDate` (`LocalDateTime`)
- `updatedAt` (`LocalDateTime`)

---

### OrderItem

**Location**: `src/main/java/com/example/bookstore/model/OrderItem.java`
**Fields**: 5

**Description**:
OrderItem entity representing an item in an order.

Links books to orders with quantity and pricing information.

**Fields**:

- `id` (`Long`)
- `order` (`Order`)
- `book` (`Book`)
- `quantity` (`Integer`)
- `unitPrice` (`BigDecimal`)

---

### Book

**Location**: `src/main/java/com/example/bookstore/model/Book.java`
**Fields**: 11

**Description**:
Book entity representing a book in the bookstore.

Contains information about books including title, author, ISBN,
price, and inventory details.

**Fields**:

- `id` (`Long`)
- `title` (`String`)
- `author` (`String`)
- `isbn` (`String`)
- `price` (`BigDecimal`)
- `description` (`String`)
- `stockQuantity` (`Integer`)
- `publishedYear` (`Integer`)
- `createdAt` (`LocalDateTime`)
- `updatedAt` (`LocalDateTime`)

---

## Model Relationships

The model relationships are determined by examining `@DBRef`, `@OneToMany`, `@ManyToOne`,
and other relationship annotations in the source code. Refer to the individual model
documentation above for specific relationship details.

## Usage Patterns

The usage patterns for these models are determined by the service layer and controller
implementations. Common patterns include:

1. **CRUD Operations**: Create, Read, Update, Delete operations for entity management
2. **Data Validation**: JSR-303 validation annotations ensure data integrity
3. **Persistence**: JPA/MongoDB annotations handle database mapping
4. **Business Logic**: Service classes orchestrate model interactions

---

**Note**: This documentation was auto-generated by analyzing the Java model files. For the most up-to-date field information, refer to the source code.